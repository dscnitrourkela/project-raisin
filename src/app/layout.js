import localFont from 'next/font/local';
import './globals.css';
import StyledComponentsRegistry from '@/lib/registry';
import GlobalStyles from '@/GlobalStyles';
import { Montserrat, Orbitron, Prompt, Share_Tech_Mono as ShareTechMono } from 'next/font/google';

// Importing a local font
const techno = localFont({
  src: '../fonts/Techno.otf',
  variable: '--font-techno',
  weight: '100 900',
});

// Google Fonts
const shareTechMono = ShareTechMono({
  subsets: ['latin'],
  weight: ['400'],
  variable: '--font-share_tech',
});

const orbitron = Orbitron({
  subsets: ['latin'],
  weight: ['400', '500', '600', '700', '800', '900'],
  variable: '--font-orbitron',
});

const montserrat = Montserrat({
  subsets: ['latin'],
  weight: ['400', '500', '600', '700', '800', '900'],
  variable: '--font-montserrat',
});

const prompt = Prompt({
  subsets: ['latin'],
  weight: ['400', '500', '600', '700', '800', '900'],
  variable: '--font-prompt',
});

// Page metadata
export const metadata = {
  title: 'Create Next App',
  description: 'Generated by create next app',
};

// Root layout component
export default function RootLayout({ children }) {
  return (
    <html lang='en'>
      <body
        className={`${techno.variable} ${shareTechMono.variable} ${orbitron.variable} ${montserrat.variable} ${prompt.variable} antialiased`}
      >
        <StyledComponentsRegistry>
          <GlobalStyles />
          {children}
        </StyledComponentsRegistry>
      </body>
    </html>
  );
}
